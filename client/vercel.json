{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ],
  "buildCommand": "npm run build",
  "installCommand": "npm install --platform=linux --arch=x64 --omit=optional",
  "framework": "vite"
}